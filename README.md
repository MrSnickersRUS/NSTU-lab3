# 📦 Лабораторная работа №3 - Структуры данных и сериализация

Реализация системы управления структурами данных на **C++** и **Go** с полной поддержкой сериализации.

## 🎯 Краткое описание

Интерактивная CLI-система для работы с 9 структурами данных:
- **Линейные**: массив, стек, очередь, односвязный/двусвязный список
- **Хеш-структуры**: HashMap (цепочки), Cuckoo Hash, Set
- **Деревья**: AVL-дерево (самобалансирующееся, только на C++)

**Особенности:**
- ✅ Бинарная и JSON сериализация
- ✅ Полное покрытие тестами (Go: 88.8%, С++: 96,4%)
- ✅ Интерактивный CLI интерфейс
- ✅ Проверка утечек памяти (C++)

---

## 📂 Структура репозитория

```
labs/
├── lab3/           # C++ версия
│   ├── src/
│   │   ├── main.cpp
│   │   ├── commands.cpp
│   │   ├── binary_serialization.h
│   │   ├── json_utils_simple.h
│   │   └── containers/       # 9 структур данных
│   ├── tests/               # Google Test
│   ├── include/
│   │   └── json.hpp         # nlohmann/json
│   ├── Makefile
│   └── README.md
│
└── lab3_go/        # Go версия
    ├── main.go
    ├── containers/          # 9 структур данных
    ├── tests/              # testing + testify
    ├── coverage.html       # 88.8% покрытие
    ├── Makefile
    └── README.md
```

---

## 🚀 Быстрый старт

### C++ версия

```bash
cd lab3

# Компиляция
make                # Обычная сборка
make debug          # С отладочной информацией
make release        # С оптимизацией -O3

# Запуск
make run            # Интерактивный режим
./lab3              # Прямой запуск

# Тестирование
make test           # Google Test
make valgrind       # Проверка утечек памяти
```

### Go версия

```bash
cd lab3_go

# Компиляция и запуск
make build          # Компиляция
make run            # Запуск

# Тестирование
make test           # 126 тестов
make coverage       # HTML отчет о покрытии (88.8%)
make html           # Открыть coverage.html
```

---

## 💻 Команды программы

Обе версии поддерживают одинаковый набор команд:

### Общие
```bash
HELP                # Справка
EXIT / QUIT         # Выход с сохранением
```

### Массив (Array)
```bash
MPUSH <name> <value>            # Добавить в конец
MINSERT <name> <idx> <value>    # Вставить по индексу
MGET <name> <index>             # Получить элемент
MSET <name> <idx> <value>       # C++: изменить элемент
AREPLACE <name> <idx> <value>   # Go: заменить элемент
MDEL <name> <index>             # C++: удалить
AREMOVE <name> <index>          # Go: удалить
MLEN <name>                     # C++: размер
ALEN <name>                     # Go: размер
MPRINT <name>                   # Вывести
```

### Стек (Stack)
```bash
SPUSH <name> <value>            # Положить в стек
SPOP <name>                     # Извлечь из стека
SPEEK <name>                    # Посмотреть вершину
SPRINT <name>                   # Вывести стек
```

### Очередь (Queue)
```bash
QPUSH <name> <value>            # C++: добавить
QENQUEUE <name> <value>         # Go: добавить
QPOP <name>                     # C++: извлечь
QDEQUEUE <name>                 # Go: извлечь
QPEEK <name>                    # Посмотреть первый
QPRINT <name>                   # Вывести
```

### Односвязный список (SingleList)
```bash
SLADDHEAD <name> <value>        # Добавить в начало
SLADDTAIL <name> <value>        # Добавить в конец
SLREMOVE <name> <value>         # Удалить элемент
SLCONTAINS <name> <value>       # Проверить наличие
SLPRINT <name>                  # Вывести
```

### Двусвязный список (DoubleList)
```bash
DLADDHEAD <name> <value>        # Добавить в начало
DLADDTAIL <name> <value>        # Добавить в конец
DLREMOVE <name> <value>         # Удалить элемент
DLCONTAINS <name> <value>       # Проверить наличие
DLPRINT <name>                  # Вывести
```

### Хеш-таблица (HashMap)
```bash
HSET <name> <key> <value>       # C++: установить
HPUT <name> <key> <value>       # Go: установить
HGET <name> <key>               # Получить значение
HDEL <name> <key>               # Удалить пару
HCONTAINS <name> <key>          # Проверить ключ
HPRINT <name>                   # Вывести все пары
```

### Множество (Set)
```bash
SETADD <name> <value>           # Добавить элемент
SETREMOVE <name> <value>        # Удалить элемент
SETCONTAINS <name> <value>      # Проверить наличие
SETPRINT <name>                 # Вывести множество
```

### AVL-дерево (Tree)
```bash
TINSERT <name> <value>          # Вставить элемент
TCONTAINS <name> <value>        # C++: проверить
TSEARCH <name> <value>          # Go: найти
TREMOVE <name> <value>          # Удалить элемент
TPRINT <name>                   # Вывести дерево
```

### Сериализация

**C++:**
```bash
SAVE_BIN <filename>                 # Все контейнеры → binary
LOAD_BIN <filename>                 # Загрузить binary
SAVE_JSON <filename>                # Все контейнеры → JSON
LOAD_JSON <filename>                # Загрузить JSON
SAVE_ARRAY_JSON <name> <file>       # Массив → JSON
LOAD_ARRAY_JSON <name> <file>       # Массив ← JSON
SAVE_HASHMAP_JSON <name> <file>     # HashMap → JSON
LOAD_HASHMAP_JSON <name> <file>     # HashMap ← JSON
```

**Go:**
```bash
SAVE <filename>                     # Все → JSON
LOAD <filename>                     # Загрузить JSON
SAVE_ARRAY <name> <file>            # Массив → JSON
LOAD_ARRAY <name> <file>            # Массив ← JSON
SAVE_HASHMAP <name> <file>          # HashMap → JSON
LOAD_HASHMAP <name> <file>          # HashMap ← JSON
SAVE_TREE <name> <file>             # Дерево → JSON
LOAD_TREE <name> <file>             # Дерево ← JSON
```

---

## 📊 Пример использования

```bash
> MPUSH numbers 10
✓ Элемент добавлен

> SPRINT stack1
Стек 'stack1' (сверху вниз): [200, 100]

> HSET users alice Alice_Wonderland
✓ Пара добавлена

> HGET users alice
Значение: Alice_Wonderland

> TINSERT bst 70
✓ Элемент вставлен

> TPRINT bst
Дерево 'bst': 30 50 70

> SAVE_JSON data.json
✓ Данные сохранены в data.json

> EXIT
Сохранить данные? (y/n): y
✓ Данные сохранены
До свидания!
```

---

## 🔬 Реализованные структуры данных

| Структура | Описание | Операции | Сложность |
|-----------|----------|----------|-----------|
| **Array** | Динамический массив | push, insert, get, set, remove | O(1) доступ, O(n) вставка |
| **Stack** | LIFO структура | push, pop, peek | O(1) все операции |
| **Queue** | FIFO структура | enqueue, dequeue, peek | O(1) все операции |
| **SingleList** | Односвязный список | addHead, addTail, remove, contains | O(n) поиск, O(1) вставка в начало |
| **DoubleList** | Двусвязный список | addHead, addTail, remove, contains | O(n) поиск, O(1) вставка с обоих концов |
| **HashMap** | Хеш-таблица (цепочки) | set, get, delete, contains | O(1) средний, O(n) худший |
| **Set** | Множество (хеш-таблица) | add, remove, contains | O(1) средний |
| **AVLTree** | Самобалансирующееся дерево | insert, search, remove | O(log n) все операции |
| **Cuckoo Hash** | Кукушкино хеширование | put, get, remove | O(1) гарантированное чтение |

---

## 🧪 Тестирование

### C++ (Google Test)

```bash
cd lab3
make test           # Запуск всех тестов
make valgrind       # Проверка утечек памяти
```

**Покрытие:**
- ✅ Основные операции всех контейнеров
- ✅ Граничные случаи (пустые, переполнение)
- ✅ Бинарная сериализация/десериализация
- ✅ JSON сериализация/десериализация

### Go (testing + testify)

```bash
cd lab3_go
make test                  # 126 тестов
make coverage              # HTML отчет
make coverage-report       # Детальный отчет
```

**Результаты:**
- ✅ **126/126 тестов** проходят
- ✅ **88.8% покрытие кода**
- ✅ HTML визуализация покрытия

---

## 📁 Форматы файлов

### Бинарный формат (`.bin`)
Компактное хранение (не читаемо человеком):
```
[magic_number][version][count][data1][data2]...
```

### JSON формат (`.json`)
Читаемый формат для обмена:
```json
{
  "arrays": {
    "numbers": [10, 20, 30]
  },
  "hashmaps": {
    "users": {
      "alice": "Alice_Wonderland",
      "bob": "Bob_Builder"
    }
  },
  "stacks": {
    "stack1": [100, 200]
  },
  "trees": {
    "bst": ["30", "50", "70"]
  }
}
```

---

## 🛠️ Make команды

### C++
```bash
make              # Компиляция
make run          # Запуск
make test         # Google Test
make valgrind     # Проверка памяти
make debug        # Сборка с -g
make release      # Сборка с -O3
make clean        # Очистка
make help         # Справка
```

### Go
```bash
make build        # Компиляция
make run          # Запуск
make test         # 126 тестов
make coverage     # HTML отчет (88.8%)
make html         # Открыть отчет
make fmt          # Форматирование
make lint         # go vet
make clean        # Очистка
make help         # Справка
```

---

## 🔄 Сравнение версий

| Аспект | C++ | Go |
|--------|-----|-----|
| **Память** | Ручное (new/delete) | Автоматическое (GC) |
| **Обобщения** | Templates | Конкретные типы (int/string) |
| **JSON** | nlohmann/json (внешняя) | encoding/json (встроенный) |
| **Тесты** | Google Test | testing + testify |
| **Покрытие** | Не реализовано | 88.8% с HTML |
| **Компиляция** | ~5-10 сек | ~1 сек |
| **Производительность** | Выше | Немного ниже |
| **Сложность кода** | Сложнее | Проще |
| **Утечки памяти** | Valgrind нужен | Невозможны (GC) |
| **Безопасность** | Ручная проверка | Встроенные проверки |

**Вывод:**
- **C++**: максимальная производительность, полный контроль памяти
- **Go**: простота, безопасность, быстрая разработка

---

## 📦 Зависимости

### C++
- **C++17** или выше
- **g++** / **clang++**
- **make**
- **Google Test** (для тестов)
- **Valgrind** (для проверки памяти)
- **nlohmann/json** (в include/)

### Go
- **Go 1.21+**
- Стандартная библиотека (никаких внешних зависимостей!)
- **testify** (опционально, для assertions)

---

## 🎓 Учебные цели

### Общие
- ✅ Реализация базовых структур данных
- ✅ Бинарная и JSON сериализация
- ✅ Написание автоматических тестов
- ✅ Создание CLI приложений
- ✅ Работа с файлами

### C++ специфика
- ✅ Работа с динамической памятью
- ✅ Использование templates
- ✅ RAII и умные указатели
- ✅ Проверка утечек памяти

### Go специфика
- ✅ Структуры и методы
- ✅ Интерфейсы
- ✅ Сборщик мусора
- ✅ Тестирование и покрытие
- ✅ Пакеты и модули

---

## 📊 Статистика проекта

### C++ версия
- **Контейнеров**: 9
- **Строк кода**: ~3000+
- **Тестов**: Полное покрытие
- **Проверка памяти**: Valgrind

### Go версия
- **Контейнеров**: 9
- **Строк кода**: ~2500+
- **Тестов**: 126
- **Покрытие**: 88.8%
- **Время компиляции**: ~1 сек

---

## 🎯 Ключевые достижения

- ✅ **Две реализации** одного проекта (C++ и Go)
- ✅ **9 структур данных** с полной функциональностью
- ✅ **Полная сериализация** (binary + JSON)
- ✅ **Автоматическое тестирование** (126 тестов в Go)
- ✅ **88.8% покрытие кода** в Go версии
- ✅ **Интерактивный CLI** с 40+ командами
- ✅ **Проверка утечек памяти** в C++
- ✅ **Идиоматичный код** для обоих языков

---

## 📚 Полезные ссылки

### C++
- [C++ Reference](https://en.cppreference.com/)
- [nlohmann/json](https://github.com/nlohmann/json)
- [Google Test](https://github.com/google/googletest)
- [Valgrind Documentation](https://valgrind.org/docs/)

### Go
- [Go Documentation](https://golang.org/doc/)
- [Go by Example](https://gobyexample.com/)
- [Testing in Go](https://golang.org/pkg/testing/)
- [testify](https://github.com/stretchr/testify)
- [Go Coverage](https://go.dev/blog/cover)
